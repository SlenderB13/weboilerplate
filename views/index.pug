block variables

doctype html
html(lang='en')
  head
    meta(charset='utf-8')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')

    base(href='/')

    title= title

    meta(name='description' content='content to be on google search')

    style
      include ../public/main.css

  body
    include ./partials/navigation.pug

    .content(data-template=template)
      block content

    script(src='main.js')
